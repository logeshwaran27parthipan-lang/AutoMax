"use client";
import React from "react";

export default function WorkflowBuilder() {
  return (
    <div className="p-4 bg-white rounded shadow">
      <h3 className="text-lg font-semibold mb-2">Workflow Builder (stub)</h3>
      <p>
        Drag and drop nodes, configure steps and triggers here. (Implement
        builder UI)
      </p>
    </div>
  );
}
